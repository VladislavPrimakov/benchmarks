[package]
name = "actix_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.5"
tokio = { version = "1", features = ["full"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
