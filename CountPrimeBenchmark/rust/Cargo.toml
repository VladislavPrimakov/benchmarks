[package]
name = "multithread_benchmark"
version = "0.1.0"
edition = "2021"

[dependencies]
# Rayon is the best Rust library for data parallelism
rayon = "1.10.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
